pipeline{
  agent{
    docker {
      image 'python:3.11-alpine'
      args '--user root -v /var/run/docker.sock:/var/run/docker.sock'
    }
  }

  stages{
    stage('Test stage'){
      steps {
      sh 'python --version'
      }
    }
  }
}